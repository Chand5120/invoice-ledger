name: invoice_ledger
version: "1.0.0"
config-version: 2

profile: invoice_ledger

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  invoice_ledger:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
